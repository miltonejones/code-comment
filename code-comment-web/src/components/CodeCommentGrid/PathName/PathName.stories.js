import React from 'react';
import PathName from './PathName';

export default {
  title: 'PathName',
  component: PathName
};

const Template = (args) => <PathName {...args} />;
export const DefaultView = Template.bind({});
DefaultView.args = {};
